<!--.col-	.col-sm-	.col-md-	.col-lg-	.col-xl- -->

<div class="container">
	{{#if retires}}
	<div class="card card-body">
		<h3 class="text-primary">Retirement</h3>
{{!-- 		{{#each users}} --}}
		<div class="card border-primary mb-2 w-100 mx-auto">
			<div class="card-header">
                <h5 class="card-title">Monthly Income: {{retires.MonthlyIncome}}</h5>
                <h5 class="card-title">Retirement Age:  {{retires.RetirementAge}}</h5>
                <h5 class="card-title">Retirement Goal: {{retires.RetirementGoal}}</h5>
		    </div>
					<div class="form-group">
                    <div class="col-xs-12">
                        <br>
                        <a href="/retire/editretire/{{retire.id}}">
						<button class="btn btn-lg btn-success"> <i class="glyphicon glyphicon-ok-sign"></i>Edit Retirement</button>
                        </a>
                    </div>
                </div>
        	</div>
{{!--             {{/each}} --}}
        </div>
	    {{else}}
	    <h3>No retirement plan set up</h3>
	{{/if}}
</div>